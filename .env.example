STAYAPI_API_KEY=
GMAIL_APP_PASSWORD=
# Path to Playwright storage state after scripts/arbitrip_login.py (default: logs/arbitrip_storage.json)
ARBITRIP_STORAGE_STATE=
# Optional — search_token from Arbitrip hotel URL query string (expires; can omit if session is valid)
ARBITRIP_SEARCH_TOKEN=
